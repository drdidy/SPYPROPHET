"""SPY Prophet — FastAPI service.

Wraps the existing Streamlit-side helpers as JSON endpoints so the Next.js
terminal at app.spyprophet.app can consume real data without re-implementing
the structure / signal / quote logic.
"""
